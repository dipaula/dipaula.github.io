jQuery(function($) {

    'use strict';

    var HOME;

    HOME = {
        init: function() {
            // this.slideshow();
        },
        testing: function() {
            $('body').each(function() {
              console.log("testing");
            });
        }
    };
    HOME.init();

});
